{
  "name": "@wtdf/fmt",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/parcel src/index.html",
    "clear": "rm -rf dist",
    "build": "yarn clear && ./node_modules/.bin/parcel build --no-source-maps src/index.html"
  },
  "devDependencies": {
    "codemirror": "^5.47.0",
    "parcel-bundler": "^1.12.3"
  }
}
